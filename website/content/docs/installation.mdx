---
title: Installation
description: Get started with Fetta in under a minute.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Installation

<Tabs items={['pnpm', 'bun', 'npm', 'yarn']}>
  <Tab value="pnpm">
```bash
pnpm add fetta
```
  </Tab>
  <Tab value="bun">
```bash
bun add fetta
```
  </Tab>
  <Tab value="npm">
```bash
npm install fetta
```
  </Tab>
  <Tab value="yarn">
```bash
yarn add fetta
```
  </Tab>
</Tabs>

**Bundle size**: ~3.4 kB (core) / ~4.3 kB (React) — minified + compressed

## Quick Start

### React

```tsx title="App.tsx"
import { SplitText } from 'fetta/react';
import { animate, stagger } from 'motion';

<SplitText
  onSplit={({ chars }) => {
    animate(chars, { opacity: [0, 1] }, { delay: stagger(0.02) });
  }}
>
  <h1>Hello world</h1>
</SplitText>
```

### Vanilla JavaScript

```ts title="main.ts"
import { splitText } from 'fetta';

const { chars } = splitText(document.querySelector('h1'));
// Animate with any library
```
